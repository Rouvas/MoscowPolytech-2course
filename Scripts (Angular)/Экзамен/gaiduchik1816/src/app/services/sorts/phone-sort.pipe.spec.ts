import { PhoneSortPipe } from './phone-sort.pipe';

describe('PhoneSortPipe', () => {
  it('create an instance', () => {
    const pipe = new PhoneSortPipe();
    expect(pipe).toBeTruthy();
  });
});
